# Local dev: http://localhost:3000
# Docker (same host): http://localhost:3000 or use your VM hostname/IP
VITE_API_URL=http://localhost:3000

# Docker Compose reads these from root .env for mysql + backend
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=plants

# Perenual API (for server/init_db.js seeding) - get key at perenual.com
PERENUAL_API_KEY=your_perenual_api_key
